import {
    userPostFetch, userLoginFetch, getProfileFetch, logoutUser,
}  from "./authAction";

import {
    allPostFetch, createPostFetch
} from './postAction';

export {
    userPostFetch, userLoginFetch, getProfileFetch, logoutUser,
    allPostFetch, createPostFetch
}